{
  "checks": {
    "disk_usage_metrics": {
      "handlers": ["graphite"],
      "type": "metric",
      "command": "/etc/sensu/plugins/disk-usage-metrics.rb --scheme stats.:::name:::",
      "subscribers": ["all"],
      "interval": 60
    }
  }
}
