{
  "checks": {
    "disk_usage__metrics": {
      "handlers": ["graphite"],
      "type": "metric",
      "command": "/etc/sensu/plugins/disk-usage-metrics.rb --scheme stats.:::name:::",
      "subscribers": ["all"],
      "interval": 60
    }
  }
}
