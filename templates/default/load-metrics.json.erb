{
  "checks": {
    "load_metrics": {
      "handlers": ["graphite"],
      "type": "metric",
      "command": "/etc/sensu/plugins/load-metrics.rb --scheme stats.:::name:::",
      "subscribers": ["all"],
      "interval": 60
    }
  }
}
