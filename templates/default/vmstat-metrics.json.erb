{
  "checks": {
    "vmstat_metrics": {
      "handlers": ["graphite"],
      "type": "metric",
      "command": "/etc/sensu/plugins/vmstat-metrics.rb --scheme stats.:::name:::",
      "subscribers": ["all"],
      "interval": 60
    }
  }
}
